#!/bin/bash

set -eux

MY_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

SMOKE_TESTS="./cf-mysql-service/proxy ./cf-mysql-service/broker ./cf-mysql-service/lifecycle"
$MY_DIR/test $SMOKE_TESTS
