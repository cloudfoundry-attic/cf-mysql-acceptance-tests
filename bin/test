#!/bin/bash

set -eux

go install -v github.com/onsi/ginkgo/ginkgo

CF_COLOR=false \
CF_VERBOSE_OUTPUT=true \
ginkgo \
    -r \
    -p \
    -v \
    -keepGoing=true \
    -randomizeSuites \
    -randomizeAllSpecs \
    -trace=true \
    -slowSpecThreshold=300 \
    -failOnPending \
    "$@"

